# Main make.code.deps file for thorn Boost

export BOOST_INSTALL_DIR
export HAVE_CAPABILITY_MPI HAVE_CAPABILITY_CUDA

$(CCTK_TARGET) $(OBJS) $(SRCS:%=%.d): $(SCRATCH_BUILD)/done/$(THORN)

ifneq ($(BOOST_BUILD),)
$(SCRATCH_BUILD)/done/$(THORN): $(SRCDIR)/build.sh
	+$(SRCDIR)/build.sh
endif
